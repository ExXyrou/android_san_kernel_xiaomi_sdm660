name: 🛠️ Build Kernel

on:
  workflow_dispatch:
    inputs:
      TELEGRAM_GROUP_ID:
        description: '💬 Telegram Group Id'
        required: true
        type: string
      DEVICE:
        description: '📱 Select Device'
        required: true
        default: 'lavender'
        type: choice
        options:
          - lavender
          - whyred
      KERNEL_BRANCH:
        description: '🌿 Select Kernel Branch'
        required: true
        default: 'st74-ksun'
        type: choice
        options:
          - st74-ksun
          - PLTS
          - st74
          - st78
      DISPLAY_BRANCH_NAME:
        description: '🏷️ Display Branch Name'
        required: true
        default: false
        type: boolean
      LOCALVERSION:
        description: '🏷️ Custom Kernel Name (e.g., MyKernel-v1.0)'
        required: false
        type: string
      LOCALVERSION_AUTO:
        description: '🔧 Enable Localversion Auto'
        required: true
        default: false
        type: boolean
      DEFAULT_HOSTNAME:
        description: '💻 Kernel Hostname (e.g., xiaomi@redmi)'
        required: true
        default: xiaomi@redmi
        type: string
      BUILD_USER:
        description: '👤 Build Username (e.g., xiaomi)'
        required: true
        default: xiaomi
        type: string
      BUILD_HOST:
        description: '🖥️ Build Hostname (e.g., redmi)'
        required: true
        default: redmi
        type: string
      QTI_HAPTICS:
        description: '📳 Include QTI Haptics Support'
        required: true
        default: false
        type: boolean
      BUILD_NEWCAM:
        description: '📷 Use Newcam Build'
        required: true
        default: false
        type: boolean
      INJECT_KSU:
        description: '🦊 Inject KernelSU'
        required: true
        default: 'none'
        type: choice
        options:
          - none
          - kowsu
          - xxksu
      FLASHABLE_TYPE:
        description: '📦 Flashable Type'
        required: true
        default: 'original'
        type: choice
        options:
          - original
          - san-kernel

env:
  KERNEL_DIR: kernel_source
  CLANG_DIR: clang
  OUT_DIR: out
  ARCH: arm64
  SUBARCH: arm64
  KERNEL_URL: https://github.com/ExXyrou/android_san_kernel_xiaomi_sdm660.git
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
  BOT_API: 8938357014:AAEV77hiQxIYxXJQLWijjzGuqsUQN1Hsu14

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout Repository & Start Timer
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Initialize Build Environment
        run: echo "BUILD_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Clone Kernel Source
        run: |
          rm -rf "${{ env.KERNEL_DIR }}"
          git clone --depth=1 -b "${{ inputs.KERNEL_BRANCH }}" "${{ env.KERNEL_URL }}" "${{ env.KERNEL_DIR }}"

      - name: Configure Kernel Version & Strings
        working-directory: ${{ env.KERNEL_DIR }}
        run: |
          CLEAN_INPUT=$(echo "${{ inputs.LOCALVERSION }}" | sed 's/^-//')
          BASE_NAME="${CLEAN_INPUT:-}"
          
          if [ "${{ inputs.DISPLAY_BRANCH_NAME }}" == "true" ]; then
            if [ -n "$BASE_NAME" ]; then
              FINAL_LOCAL="-${{ inputs.KERNEL_BRANCH }}-${BASE_NAME}"
            else
              FINAL_LOCAL="-${{ inputs.KERNEL_BRANCH }}"
            fi
          else
            if [ -n "$BASE_NAME" ]; then
              FINAL_LOCAL="-${BASE_NAME}"
            else
              FINAL_LOCAL=""
            fi
          fi
          
          echo "KERNEL_NAME_STRING=$FINAL_LOCAL" >> $GITHUB_ENV
          echo "Kernel string configured: $FINAL_LOCAL"

          KVER_MAJOR=$(grep -m1 '^VERSION =' Makefile | tr -d ' ' | cut -d'=' -f2)
          KVER_PATCH=$(grep -m1 '^PATCHLEVEL =' Makefile | tr -d ' ' | cut -d'=' -f2)
          KVER_SUB=$(grep -m1 '^SUBLEVEL =' Makefile | tr -d ' ' | cut -d'=' -f2)
          FULL_KVER="${KVER_MAJOR}.${KVER_PATCH}.${KVER_SUB}"
          echo "KERNEL_VERSION=$FULL_KVER" >> $GITHUB_ENV
          echo "Detected version: $FULL_KVER"

      - name: Install Build Dependencies
        timeout-minutes: 10
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          while sudo fuser /var/lib/dpkg/lock-frontend >/dev/null 2>&1; do sleep 5; done
          for i in 1 2 3; do sudo apt-get update -yq && break; sleep 10; done
          sudo apt-get install -yq --no-install-recommends \
            -o Acquire::Retries=3 \
            bc bison libssl-dev libelf-dev build-essential python3 python3-pip curl git zip unzip wget ccache libncurses-dev rsync gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu gcc-arm-linux-gnueabi binutils-arm-linux-gnueabi xz-utils

      - name: Setup Compiler Cache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-eas-${{ inputs.DEVICE }}-${{ github.run_id }}
          restore-keys: |
            ccache-eas-${{ inputs.DEVICE }}-

      - name: Setup KernelSU
        if: ${{ inputs.INJECT_KSU != 'none' }}
        working-directory: ${{ env.KERNEL_DIR }}
        run: |
          if [ "${{ inputs.INJECT_KSU }}" == "kowsu" ]; then
            export KSU_EXPECTED_SIZE="0x375"
            export KSU_EXPECTED_HASH="484fcba6e6c43b1fb09700633bf2fb4758f13cb0b2f4457b80d075084b26c588"
            export KSU_MANAGER_PACKAGE="com.kowx712.supermanager"
          fi
          curl -LSs "https://raw.githubusercontent.com/backslashxx/KernelSU/refs/heads/master/kernel/setup.sh" | bash -s master
          
          KSU_TAG=$(curl -s https://api.github.com/repos/backslashxx/KernelSU/releases/latest | grep -o '"tag_name": *"[^"]*"' | head -n 1 | cut -d'"' -f4)
          KSU_URL="${KSU_TAG:+https://github.com/backslashxx/KernelSU/releases/tag/$KSU_TAG}"
          echo "KSU_LINK=${KSU_URL:-https://github.com/backslashxx/KernelSU/releases}" >> $GITHUB_ENV

      - name: Setup Clang Toolchain
        id: clang-cache
        uses: actions/cache@v4
        with:
          path: ${{ env.CLANG_DIR }}
          key: clang-zyc-v2-${{ hashFiles('.github/workflows/*.yml') }}

      - name: Download & Extract Clang 15.0.7
        if: steps.clang-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p "$CLANG_DIR"
          CLANG_URL=$(curl -s https://api.github.com/repos/ZyCromerZ/Clang/releases | grep -o "https://github.com/ZyCromerZ/Clang/releases/download/.*/Clang-15.0.7-.*\.tar\.gz" | head -n 1)
          curl -LSs "$CLANG_URL" -o zyc_clang.tar.gz
          tar -zxf zyc_clang.tar.gz -C "$CLANG_DIR" && rm -f zyc_clang.tar.gz
          
          if [ ! -d "$CLANG_DIR/bin" ]; then
            CLANG_SUBDIR=$(find "$CLANG_DIR" -mindepth 1 -maxdepth 1 -type d | head -n 1)
            [ -n "$CLANG_SUBDIR" ] && mv "$CLANG_SUBDIR"/* "$CLANG_DIR/"
          fi
          echo "CLANG_VERSION=ZyC Clang 15.0.7" >> $GITHUB_ENV

      - name: Prepare Configs & Build Options
        working-directory: ${{ env.KERNEL_DIR }}
        run: |
          [ -f kernel/sched/sched.h ] && sed -i 's/cpu_rq(cpu)->cfs->cumulative_runnable_avg/cpu_rq(cpu)->cfs.cumulative_runnable_avg/g' kernel/sched/sched.h
          
          if [ "${{ inputs.DEVICE }}" == "whyred" ]; then
            DEFCONFIG="whyred_defconfig"
          else
            DEFCONFIG="lavender_defconfig"
          fi
          
          export PATH="$GITHUB_WORKSPACE/$CLANG_DIR/bin:$PATH"
          make O=$OUT_DIR ARCH=${{ env.ARCH }} CC="$GITHUB_WORKSPACE/$CLANG_DIR/bin/clang" CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- $DEFCONFIG

          enable_config() { ./scripts/config --file "$OUT_DIR/.config" --enable "$1"; }
          disable_config() { ./scripts/config --file "$OUT_DIR/.config" --disable "$1"; }
          set_str() { ./scripts/config --file "$OUT_DIR/.config" --set-str "$1" "$2"; }
          
          set_str CONFIG_DEFAULT_HOSTNAME "${{ inputs.DEFAULT_HOSTNAME }}"
          set_str CONFIG_LOCALVERSION "${{ env.KERNEL_NAME_STRING }}"
          
          if [ "${{ inputs.LOCALVERSION_AUTO }}" == "true" ]; then
            enable_config CONFIG_LOCALVERSION_AUTO
            git config --global --add safe.directory "$PWD"
            git update-index --assume-unchanged $(git ls-files) || true
          else
            disable_config CONFIG_LOCALVERSION_AUTO
          fi

          if [ "${{ inputs.FLASHABLE_TYPE }}" == "original" ]; then
            enable_config CONFIG_KERNEL_GZIP && disable_config CONFIG_KERNEL_XZ
            enable_config CONFIG_RD_GZIP && disable_config CONFIG_RD_XZ
            sed -i 's/CONFIG_BUILD_ARM64_APPENDED_KERNEL_IMAGE_NAME="Image.xz-dtb"/CONFIG_BUILD_ARM64_APPENDED_KERNEL_IMAGE_NAME="Image.gz-dtb"/g; s/CONFIG_BUILD_ARM64_APPENDED_KERNEL_IMAGE_NAME="Image.xz"/CONFIG_BUILD_ARM64_APPENDED_KERNEL_IMAGE_NAME="Image.gz-dtb"/g' "$OUT_DIR/.config"
          else
            disable_config CONFIG_KERNEL_GZIP && enable_config CONFIG_KERNEL_XZ
            enable_config CONFIG_RD_XZ
            sed -i 's/CONFIG_BUILD_ARM64_APPENDED_KERNEL_IMAGE_NAME="Image.gz-dtb"/CONFIG_BUILD_ARM64_APPENDED_KERNEL_IMAGE_NAME="Image.xz"/g' "$OUT_DIR/.config"
          fi

          [ "${{ inputs.QTI_HAPTICS }}" == "true" ] && enable_config CONFIG_INPUT_QTI_HAPTICS
          [ "${{ inputs.BUILD_NEWCAM }}" == "true" ] && enable_config CONFIG_XIAOMI_NEW_CAMERA_BLOBS
          
          if [ "${{ inputs.INJECT_KSU }}" != "none" ]; then
            enable_config CONFIG_KSU
            enable_config CONFIG_KSU_TAMPER_SYSCALL_TABLE
          fi

          make O=$OUT_DIR ARCH=${{ env.ARCH }} CC="$GITHUB_WORKSPACE/$CLANG_DIR/bin/clang" CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- olddefconfig

      - name: Build Kernel
        working-directory: ${{ env.KERNEL_DIR }}
        run: |
          export PATH="/usr/lib/ccache:$GITHUB_WORKSPACE/$CLANG_DIR/bin:$PATH"
          export KBUILD_BUILD_USER="${{ inputs.BUILD_USER }}"
          export KBUILD_BUILD_HOST="${{ inputs.BUILD_HOST }}"
          
          make -j$(nproc --all) O=$OUT_DIR ARCH=${{ env.ARCH }} \
            CC="ccache $GITHUB_WORKSPACE/$CLANG_DIR/bin/clang" \
            HOSTCC="ccache $GITHUB_WORKSPACE/$CLANG_DIR/bin/clang" \
            HOSTCXX="ccache $GITHUB_WORKSPACE/$CLANG_DIR/bin/clang++" \
            AR="$GITHUB_WORKSPACE/$CLANG_DIR/bin/llvm-ar" \
            NM="$GITHUB_WORKSPACE/$CLANG_DIR/bin/llvm-nm" \
            OBJCOPY="$GITHUB_WORKSPACE/$CLANG_DIR/bin/llvm-objcopy" \
            OBJDUMP="$GITHUB_WORKSPACE/$CLANG_DIR/bin/llvm-objdump" \
            STRIP="$GITHUB_WORKSPACE/$CLANG_DIR/bin/llvm-strip" \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            KCFLAGS="-Wno-address -Wno-implicit-function-declaration" 2>&1 | tee build.log

          if ! find "$OUT_DIR/arch/arm64/boot" -maxdepth 1 -name "Image*" | grep -q .; then
            echo "Error: Kernel output image not found!"
            exit 1
          fi

      - name: Package Kernel Zip
        id: img
        working-directory: ${{ env.KERNEL_DIR }}
        run: |
          IMAGE=$(find "$PWD/$OUT_DIR/arch/arm64/boot" -type f \( -name "Image.gz-dtb" -o -name "Image.gz" -o -name "Image.xz" -o -name "Image" \) | head -n1)
          
          if [ "${{ inputs.FLASHABLE_TYPE }}" == "original" ]; then
            BRANCH_NAME="original"
            OUTPUT_NAME="Image.gz-dtb"
            if [[ "$IMAGE" == *Image.gz ]]; then
              cp "$IMAGE" "$GITHUB_WORKSPACE/$OUTPUT_NAME"
            else
              gzip -f -k -c "$IMAGE" > "$GITHUB_WORKSPACE/$OUTPUT_NAME"
            fi
          else
            BRANCH_NAME="san-kernel"
            OUTPUT_NAME="Image.xz"
            if [[ "$IMAGE" == *.xz ]]; then
              cp "$IMAGE" "$GITHUB_WORKSPACE/$OUTPUT_NAME"
            else
              xz -f -k -c "$IMAGE" > "$GITHUB_WORKSPACE/$OUTPUT_NAME"
            fi
          fi
          
          cd "$GITHUB_WORKSPACE"
          git clone --depth=1 -b "$BRANCH_NAME" https://github.com/ExXyrou/AnyKernel3 AnyKernel3
          cp "$GITHUB_WORKSPACE/$OUTPUT_NAME" AnyKernel3/
          
          cd AnyKernel3
          if [ -f anykernel.sh ]; then
            if [ "${{ inputs.FLASHABLE_TYPE }}" == "san-kernel" ]; then
              sed -i 's/Image\.gz-dtb/Image.xz/g; s/Image\.gz/Image.xz/g; s/Image$/Image.xz/g' anykernel.sh
            else
              sed -i 's/Image\.xz/Image.gz-dtb/g; s/Image\.gz/Image.gz-dtb/g; s/Image$/Image.gz-dtb/g' anykernel.sh
            fi
            grep -q "kernel_name=" anykernel.sh && sed -i "s/^kernel_name=.*/kernel_name=\"$OUTPUT_NAME\"/" anykernel.sh
          fi
          
          FINAL_LOCALVER=$(echo "${{ env.KERNEL_NAME_STRING }}" | sed 's/^-//')
          sed -i -E 's/^(kernel\.string=).*/\1'"$FINAL_LOCALVER"'/' anykernel.sh

          sed -i "s/^device\.name1=.*/device.name1=${{ inputs.DEVICE }}/" anykernel.sh
          
          ZIP_NAME="${{ inputs.DEVICE }}-${{ env.KERNEL_VERSION }}{{ env.KERNEL_NAME_STRING }}-$(date +'%Y%m%d').zip"
          zip -r9 "$ZIP_NAME" * -x ".git/*"
          
          echo "zip_path=$GITHUB_WORKSPACE/AnyKernel3/$ZIP_NAME" >> "$GITHUB_OUTPUT"

      - name: Send Telegram Notification (Success)
        if: success()
        run: |
          DURATION=$(( $(date +%s) - ${{ env.BUILD_START_TIME }} ))
          DURATION_STR="$((DURATION / 60))m $((DURATION % 60))s"
          CAM_TYPE=$([ "${{ inputs.BUILD_NEWCAM }}" == "true" ] && echo "newcam" || echo "oldcam")
          
          if [ "${{ inputs.KERNEL_BRANCH }}" == "st74-ksun" ]; then
            KSU_TEXT='<a href="https://t.me/exxyrouprojectfile/70?single">ksun</a>'
          elif [ "${{ inputs.INJECT_KSU }}" == "kowsu" ]; then
            KSU_TEXT='<a href="https://t.me/kowsu_build">kowsu</a>'
          elif [ "${{ inputs.INJECT_KSU }}" == "xxksu" ]; then
            KSU_TEXT='<a href="${{ env.KSU_LINK }}">xxksu</a>'
          else
            KSU_TEXT='<code>none</code>'
          fi

          MESSAGE=$(printf '✅ <b>BUILD SUCCESS</b>\n\n📱 <b>Device:</b> <code>%s</code>\n🏷️ <b>Kernel Name:</b> <code>%s%s</code>\n📷 <b>Build Type:</b> <code>%s</code>\n📳 <b>QTI Haptics:</b> <code>%s</code>\n🦊 <b>KSU:</b> %s\n⏱️ <b>Time:</b> <code>%s</code>' "${{ inputs.DEVICE }}" "${{ env.KERNEL_VERSION }}" "${{ env.KERNEL_NAME_STRING }}" "$CAM_TYPE" "${{ inputs.QTI_HAPTICS }}" "$KSU_TEXT" "$DURATION_STR")
          
          curl -s -X POST "https://api.telegram.org/bot${{ env.BOT_API }}/sendDocument" \
            -F chat_id="${{ inputs.TELEGRAM_GROUP_ID }}" \
            -F document="@${{ steps.img.outputs.zip_path }}" \
            -F parse_mode="HTML" \
            -F caption="$MESSAGE"

      - name: Send Telegram Notification (Failure)
        if: failure()
        run: |
          DURATION=$(( $(date +%s) - ${{ env.BUILD_START_TIME }} ))
          DURATION_STR="$((DURATION / 60))m $((DURATION % 60))s"
          CAM_TYPE=$([ "${{ inputs.BUILD_NEWCAM }}" == "true" ] && echo "newcam" || echo "oldcam")
          
          if [ "${{ inputs.KERNEL_BRANCH }}" == "st74-ksun" ]; then
            KSU_TEXT='<a href="https://t.me/exxyrouprojectfile/70?single">ksun</a>'
          elif [ "${{ inputs.INJECT_KSU }}" == "kowsu" ]; then
            KSU_TEXT='<a href="https://t.me/kowsu_build">kowsu</a>'
          elif [ "${{ inputs.INJECT_KSU }}" == "xxksu" ]; then
            KSU_TEXT='<a href="${{ env.KSU_LINK }}">xxksu</a>'
          else
            KSU_TEXT='<code>none</code>'
          fi

          MESSAGE=$(printf '❌ <b>BUILD FAILED</b>\n\n📱 <b>Device:</b> <code>%s</code>\n🏷️ <b>Kernel Name:</b> <code>%s%s</code>\n⏱️ <b>Time:</b> <code>%s</code>\n\n💥 <i>Check build.log for details.</i>' "${{ inputs.DEVICE }}" "${{ env.KERNEL_VERSION }}" "${{ env.KERNEL_NAME_STRING }}" "$DURATION_STR")
          
          curl -s -X POST "https://api.telegram.org/bot${{ env.BOT_API }}/sendDocument" \
            -F chat_id="${{ inputs.TELEGRAM_GROUP_ID }}" \
            -F document="@${{ env.KERNEL_DIR }}/build.log" \
            -F parse_mode="HTML" \
            -F caption="$MESSAGE"
